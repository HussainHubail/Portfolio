export const features = {
  // Existing UX features (keep enabled by default)
  scrollProgressBar: true,
  backToTop: true,
  animatedAccent: true,

  // New additive features
  hashDeepLinks: true,
  spotlightCursor: true,
  konamiMode: true,
};
